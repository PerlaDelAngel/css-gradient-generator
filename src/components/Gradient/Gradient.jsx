
export default function Gradient({values}) {
  const defaultColor = values.color1;
  const gradientType = `${values.gradType}-gradient(${values.direction},${values.color1}, ${values.color2})`

  /* const styles = {
    backgroudColor: defaultColor,
    backgroud: gradientType
  } */

  return (
    <div></div>
  )
}
